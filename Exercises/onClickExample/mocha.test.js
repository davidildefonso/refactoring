
//add this to :

// pckage json :: scripts : test: node ./node_modules/mocha/bin/mocha"


// const chai = require("chai");
// const expect = chai.expect;
// // import sinon
// const sinon = require("sinon");
// const getIndexPage = require("./first").getIndexPage
// const delayReturn = require('./first').delayReturn
// const timerGame = require('./first').timerGame;
  


// var FakeTimers = require("@sinonjs/fake-timers");
// var clock = FakeTimers.createClock();


// describe("getIndexPage", function() {
//   it("should return index page", function() {

//     let callBack = () => console.log("Hello") //sinon.spy()

//     getIndexPage(callBack)
//   	// let's see what we get on res.send
//     clock.tick(2000)
//     // `res.send` called once
//     expect(callBack.calledOnce).to.be.true;
//     // expect to get argument `bla` on first call
//     //expect(callBack.firstCall.args[0]).to.equal("Hey");
//   })
// })

// describe("delayReturn", () => {
// 	it("should return a delay message 'Hello World'", () => {
// 		const spyFunc = sinon.spy()
// 		delayReturn()
// 		expect(spyFunc.calledOnce).to.be.true;
// 	})
// })




// it('waits 1 second before ending the game', () => {
//   timerGame();
// 	expect(callBack.calledOnce).to.be.true;
//   //expect(setTimeout).toHaveBeenCalledTimes(1);
//   //expect(setTimeout).toHaveBeenLastCalledWith(expect.any(Function), 1000);
// });

